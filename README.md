# Head Tracking Window Activator

## Build

tested on Mac OS X (10.11) only.

```
sudo gem install cocoapods
brew tap homebrew/science
brew install opencv
```

and in the repository:

```
pod install
```

and open workspace.
